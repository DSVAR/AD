@using AD.BLL.ModelsDTO
@model List<UserViewModel>

@{
    ViewData["Title"] = "Role";

}

<form method="post" asp-controller="Home">

    <div>
        <input type="text" name="fullname" placeholder="Фамилия" />
        <button asp-action="Find" type="submit">Найти</button>
    </div>



    <div class="grid-are ">
        <div class="colum">
            <p>Пользователь</p>
        </div>

        @*<div class="colum">
            <p>Дать права</p>
        </div>
        <div class="colum">
            <p>Забрать права</p>
        </div>*@




        @foreach (var user in Model)
        {
            <div class="colum item">
                <p class="child">@user.UserName</p>
                <p class="child">Fullname</p>
                <p class="child">email</p>
                <p class="child">phone</p>
            </div>
            @*if (!user.IsAdmin)
            {
                <div class="colum">
                    <button class="btn  btn-dark" asp-action="Creation" asp-route-email="@user.Email"> Дать права</button>
                </div>
                <div class="colum">
                    <button disabled class=" btn btn-primary" id="@user.Id" name="removePer"> Забрать права</button>
                </div>
            }
            else
            {
                <div class="colum">
                    <button disabled class="btn  btn-dark"> Дать права</button>
                </div>
                <div class="colum">
                    <button class="btn btn-primary" type="submit" onclick="btn()" id="@user.Id"> Забрать права</button>
                </div>
            }*@
        }

    </div>
    <button id="swer">sw</button>
</form>


<script src="~/lib/jquery/dist/jquery.min.js"></script>

<script>
    
    function btn(event) {
        function() {
            alert(this.id)
        }

    }

    $(document).ready(function () {
        $('.btn').click(function () {
            console.log(this.id);
            debugger;
        });
    });

</script>